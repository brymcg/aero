---
title: "Aero Blog"
has_hero: false
---

%main
  %section{ id: "section-blogindex" }
    %h1
      The Aero Blog
    .wrapper
    
      .posts
        %h3
          Recent Posts

        - page_articles.each do |a|
          %article.blogsumm
            %h4.name
              = link_to a.title, a
            .date
              = a.date.strftime('%b %e, %Y')
            .text
              = a.summary(500)
            .link
              = link_to "Read on...", a.title

      %aside.tags
        %h3
          Tags

        - blog.tags.each do |tag, articles|
          %ul
            %li<>
              = link_to "#{tag} (#{articles.size})", tag_path(tag)






